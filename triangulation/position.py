class Position:
    maxNode
    nodes = []
    maxInten = 0
    time = 0

    def __init__(self, nodes):
        closest = -1
        self.nodes = nodes
        for index, node in enumerate(self.nodes):
            if node.getTime > self.time
                closest = index         
        

        closestNode = self.nodes.pop(index)

    def calculatePos(self):
        for node in self.nodes

